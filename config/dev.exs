import Config

# Development-specific configuration
# JSON logging is active in dev (inherited from config.exs) for consistency
# with production log format. Use `mix run -e "Logger.info(\"test\")"` and
# pipe through `jq` for human-readable output when debugging.
